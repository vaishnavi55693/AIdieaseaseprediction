Synthetic datasets for all five diseases are generated by running:

```bash
python generate_synthetic_data.py
```

This creates one CSV per disease in this folder, ready for `models/train_models.py`.
